# tiller-ai

This is the codebase to develop tiller-ai. 

After running /dock on a feature branch, run `npm run regen` and check if the output of the generated files matches wwhat you would expect. if that's the case, commit it, before finishing /dock.
